<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FlowBoard | Orchestrate Your Team's Success</title>
    <style>
        /* CSS STYLES - The Visual Layer */
        :root {
            --primary-blue: #1a73e8;
            --dark-navy: #0d1b2a;
            --light-gray: #f8f9fa;
            --text-main: #333;
            --white: #ffffff;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            color: var(--text-main);
            line-height: 1.6;
        }

        /* Navigation */
        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1.5rem 10%;
            background: var(--white);
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .logo {
            font-size: 1.5rem;
            font-weight: bold;
            color: var(--primary-blue);
        }

        /* Hero Section */
        header {
            background: linear-gradient(135deg, var(--dark-navy) 0%, var(--primary-blue) 100%);
            color: var(--white);
            padding: 100px 10%;
            text-align: center;
        }

        header h1 {
            font-size: 3rem;
            margin-bottom: 1rem;
        }

        header p {
            font-size: 1.2rem;
            margin-bottom: 2rem;
            opacity: 0.9;
        }

        .cta-button {
            background-color: var(--white);
            color: var(--primary-blue);
            padding: 15px 30px;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            transition: transform 0.2s;
            display: inline-block;
        }

        .cta-button:hover {
            transform: scale(1.05);
        }

        /* Features Section */
        #features {
            padding: 80px 10%;
            background: var(--white);
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 30px;
        }

        .feature-card {
            padding: 30px;
            border: 1px solid #eee;
            border-radius: 10px;
            text-align: center;
            transition: box-shadow 0.3s;
        }

        .feature-card:hover {
            box-shadow: 0 10px 20px rgba(0,0,0,0.05);
        }

        .feature-card h3 {
            color: var(--primary-blue);
            margin-bottom: 15px;
        }

        /* Footer */
        footer {
            background: var(--dark-navy);
            color: var(--white);
            padding: 40px 10%;
            text-align: center;
            font-size: 0.9rem;
        }
    </style>
</head>
<body>

    <nav>
        <div class="logo">FlowBoard</div>
        <div>
            <a href="#features" style="text-decoration: none; color: var(--text-main); margin-right: 20px;">Features</a>
            <a href="#" class="cta-button" style="padding: 10px 20px; background: var(--primary-blue); color: white;">Get Started</a>
        </div>
    </nav>

    <header>
        <h1>Orchestrate Your Team's Success</h1>
        <p>The all-in-one task orchestration tool designed for modern remote teams. <br>Move faster, stay aligned, and hit your goals with FlowBoard.</p>
        <a href="#" class="cta-button">Launch Your First Board</a>
    </header>

    <section id="features">
        <div class="feature-card">
            <h3>Smart Automation</h3>
            <p>Eliminate repetitive tasks with custom workflows that trigger actions automatically across your team.</p>
        </div>
        <div class="feature-card">
            <h3>Real-time Collaboration</h3>
            <p>Chat, share files, and update statuses in a unified workspace that keeps everyone in the loop.</p>
        </div>
        <div class="feature-card">
            <h3>Total Visibility</h3>
            <p>Advanced dashboards give you a bird's-eye view of project health and team bandwidth at a glance.</p>
        </div>
    </section>

    <footer>
        <p>&copy; 2026 FlowBoard Task Orchestration. Built with Gemini for IPL Tech Lab.</p>
    </footer>

</body>
</html>